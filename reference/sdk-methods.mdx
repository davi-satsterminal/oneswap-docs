---
title: "SDK methods"
description: "The OneSwap client surface for escrow swaps."
---

The SDK currently exposes exactly one namespace: `escrow`.

## Constructor

```typescript
import { OneSwap } from '@oneswap/sdk'

const oneswap = new OneSwap({ apiKey: 'sk_live_...' })
```

<ParamField path="apiKey" type="string" required>
  Your integrator API key (`sk_live_` followed by 48 hex chars). Sent on every escrow request as
  the `x-sdk-key` header. Keep it server-side only.
</ParamField>

<ParamField path="environment" type="'mainnet' | 'devnet'" default="mainnet">
  Which OneSwap backend to use. The SDK resolves the URL itself — `mainnet`
  (`https://api.oneswap.cc/swapv2`) or `devnet` (`https://devnet.api.oneswap.cc/swapv2`). You never
  pass a raw URL.
</ParamField>

<ParamField path="timeout" type="number" default="30000">
  Per-request timeout in milliseconds.
</ParamField>

```typescript
// Point at devnet for integration testing
const oneswap = new OneSwap({
  apiKey: 'sk_live_...',
  environment: 'devnet',
})
```

## `escrow.createSwap`

Create an escrow swap. Returns an [`EscrowSwap`](/reference/types#escrowswap) in status `awaiting_deposit`, carrying the `depositParty` your end-user must fund and the `deadline` they must beat.

```typescript
oneswap.escrow.createSwap(args: {
  userRef: string
  inSymbol: string
  amountIn: number
  outSymbol: string
  minOut?: number
  slippageBps?: number
}): Promise<EscrowSwap>
```

<ParamField path="userRef" type="string" required>
  Your stable id for the end-user. Only one open swap per `userRef` at a time.
</ParamField>

<ParamField path="inSymbol" type="string" required>
  The token the user deposits, e.g. `'CBTC'`.
</ParamField>

<ParamField path="amountIn" type="number" required>
  Decimal amount of `inSymbol` to swap. The deposit must arrive within **1%** of this amount.
</ParamField>

<ParamField path="outSymbol" type="string" required>
  The token to return to the user, e.g. `'HTEST'`.
</ParamField>

<ParamField path="minOut" type="number">
  Optional hard floor on the output amount. If the swap would return less than this, it is refunded.
</ParamField>

<ParamField path="slippageBps" type="number" default="200">
  Optional slippage tolerance in basis points. Defaults to `200` (2%).
</ParamField>

```typescript
const swap = await oneswap.escrow.createSwap({
  userRef: 'user-1042',
  inSymbol: 'CBTC',
  amountIn: 0.01,
  outSymbol: 'HTEST',
  minOut: 0.95,
  slippageBps: 100,
})

console.log(swap.id)           // 'esc_...'
console.log(swap.status)       // 'awaiting_deposit'
console.log(swap.depositParty) // the party the user must fund
console.log(swap.deadline)     // ISO timestamp
```

Throws if `{ inSymbol, outSymbol }` has no visible pool (`no pool for IN/OUT`) or if the `userRef` already has an open swap. See [Error handling](/guides/error-handling).

## `escrow.getSwap`

Fetch the current state of a swap by id. Only returns swaps owned by your integrator.

```typescript
oneswap.escrow.getSwap(id: string): Promise<EscrowSwap>
```

<ParamField path="id" type="string" required>
  The swap id returned by `createSwap` (`esc_...`).
</ParamField>

```typescript
const swap = await oneswap.escrow.getSwap('esc_...')
console.log(swap.status)
```

## `escrow.waitForSwap`

Poll a swap until it reaches a terminal status (`returned`, `refunded`, `expired`, or `failed`), or until the local timeout. Resolves with the final [`EscrowSwap`](/reference/types#escrowswap).

```typescript
oneswap.escrow.waitForSwap(id: string, opts?: {
  pollMs?: number
  timeoutMs?: number
}): Promise<EscrowSwap>
```

<ParamField path="id" type="string" required>
  The swap id to wait on.
</ParamField>

<ParamField path="opts.pollMs" type="number" default="3000">
  Poll interval in milliseconds.
</ParamField>

<ParamField path="opts.timeoutMs" type="number" default="900000">
  Give up after this many milliseconds (default 15 minutes). On timeout the swap may still be
  settling — resume with `getSwap(id)`.
</ParamField>

```typescript
const done = await oneswap.escrow.waitForSwap('esc_...', {
  pollMs: 3000,
  timeoutMs: 900000,
})

if (done.status === 'returned') {
  console.log(`Returned ${done.amountOut} ${done.outSymbol} to ${done.senderParty}`)
}
```
